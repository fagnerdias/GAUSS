func mdc(int n, int m, int r) return int is begin

	if (m == 0) then
		r = n;
		return r;
	else then
		int aux = n%m;	
		return mdc(m,aux, r);
	endif


end mdc

main is begin
	int a;
	int b;
	int c = 90;


	printf(%s, "Digite o primeiro numero Inteiro e Natural:\n");
	scanf(%i,a);
	if (a<1) then
		printf(%s, "O numero deveria ser positivo\n");
	endif

	printf(%s,"Digite o segundo numero Inteiro e Natural:\n");

	scanf(%i,b);
	if (b<1) then
		printf(%s, "O numero deveria ser positivo\n");
	endif

	

	int v = mdc(a,b, c);
	printf(%s, "O mdc é: ");
	printf(%i, v);
	printf(%s, "\n");


	
	return 0;
end